{
  "rules": {
    "roles": {
      ".read": "auth != null && auth.token.email != null && root.child('roles').child(auth.token.email.toLowerCase().replace('.', ',')).val() == 'admin'",
      ".write": "auth != null && auth.token.email != null && root.child('roles').child(auth.token.email.toLowerCase().replace('.', ',')).val() == 'admin'",
      "$ek": {
        ".read": "auth != null && auth.token.email != null && $ek == auth.token.email.toLowerCase().replace('.', ',')",
        ".write": "auth != null && auth.token.email != null && auth.token.email_verified == true && auth.token.email.endsWith('@lsps.tp.edu.tw') && $ek == auth.token.email.toLowerCase().replace('.', ',') && !data.exists() && (newData.val() == 'edu' || newData.val() == 'stu' || newData.val() == 'cou')",
        ".validate": "newData.val() == 'edu' || newData.val() == 'stu' || newData.val() == 'cou' || newData.val() == 'admin'"
      }
    },
    "award_seats": {
      ".read": "auth != null && auth.token.email != null && auth.token.email_verified == true && auth.token.email.endsWith('@lsps.tp.edu.tw')",
      "$date": {
        "$seat": {
          ".write": "auth != null && auth.token.email != null && auth.token.email_verified == true && auth.token.email.endsWith('@lsps.tp.edu.tw') && ( root.child('roles').child(auth.token.email.toLowerCase().replace('.', ',')).val() == 'admin' || (newData.exists() && newData.child('dept').val() == root.child('roles').child(auth.token.email.toLowerCase().replace('.', ',')).val() && (!data.exists() || data.child('dept').val() == root.child('roles').child(auth.token.email.toLowerCase().replace('.', ',')).val())) || (!newData.exists() && data.child('dept').val() == root.child('roles').child(auth.token.email.toLowerCase().replace('.', ',')).val()) )",
          ".validate": "newData.hasChildren(['dept'])",
          "dept": { ".validate": "newData.val() == 'edu' || newData.val() == 'stu' || newData.val() == 'cou'" },
          "award": { ".validate": "newData.isString() && newData.val().length <= 40" },
          "name": { ".validate": "newData.isString() && newData.val().length <= 40" },
          "classNo": { ".validate": "newData.isString() && newData.val().length <= 20" },
          "$other": { ".validate": false }
        }
      }
    },
    "history": {
      ".read": "auth != null && auth.token.email != null && auth.token.email_verified == true && auth.token.email.endsWith('@lsps.tp.edu.tw')",
      "$date": {
        ".write": "auth != null && auth.token.email != null && auth.token.email_verified == true && auth.token.email.endsWith('@lsps.tp.edu.tw')"
      }
    }
  }
}
